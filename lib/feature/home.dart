import 'package:flutter/material.dart';

class HomeVerify extends StatelessWidget {
  const HomeVerify({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Your are Verified',style: TextStyle(fontSize: 18.0),),
    );
  }
}